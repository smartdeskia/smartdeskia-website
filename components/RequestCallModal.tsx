"use client";
export default function RequestCallModal({ onClose }: { onClose: () => void }) {
  return <div className="overlay" role="presentation" onKeyDown={event => event.key === "Escape" && onClose()} onMouseDown={event => event.target === event.currentTarget && onClose()}><div className="modal" role="dialog" aria-modal="true" aria-labelledby="request-title"><button className="close" aria-label="Close request form" onClick={onClose}>×</button><p className="mono coral">REQUEST A CALL</p><h2 id="request-title">Let’s talk about<br /><em>your front desk.</em></h2><p>Leave your details and we’ll show you how Sofia could work for your business.</p><form action={process.env.NEXT_PUBLIC_REQUEST_FORM_URL || "#"} method="POST"><label>Your name<input name="name" required placeholder="Alex Morgan" /></label><label>Phone number<input name="phone" required placeholder="+356 99 000 000" /></label><button className="coral-button">Request my call <b>↗</b></button></form></div></div>;
}
